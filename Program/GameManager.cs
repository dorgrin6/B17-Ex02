using System;
using System.Collections.Generic;

namespace Program
{
    class GameManager
    {
        private static List<char> m_guessesArray;

        public static void GetComputerGuesses(List<char> io_GuessesArray)
        {
            Random chosenChar = new Random();

            foreach (char element in io_GuessesArray)
            {
                element =
            }
        }
    }
}
